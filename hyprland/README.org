#+TITLE: Hyprland Configuration
#+AUTHOR: Richard Redcroft
#+EMAIL: Richard@Redcroft.tech
#+OPTIONS: toc:nil num:nil
#+PROPERTY: Header-args :tangle-mode (identity #o444) :mkdirp yes
#+auto_tangle: t

* Hyprland

** Monitors
#+begin_src conf :tangle (if (string= system-name "heimdall") "~/.config/hypr/hyprland.conf" "")
  monitor=DP-1,2560x1440@240,0x0,1,bitdepth,10,vrr,1
  monitor=HDMI-A-1,480x1920@60,2560x0,1, transform, 3
  exec-once = openrgb --profile "solid-orange.orp"
#+end_src

#+begin_src conf :tangle (if (string= system-name "red") "~/.config/hypr/hyprland.conf" "")
  monitor=eDP-1,1920x1080,0x0,1
#+end_src

#+begin_src conf :tangle (if (string= system-name "poseidon") "~/.config/hypr/hyprland.conf" "")
  monitor=DP-1,3840x2160,3840x0,1
  monitor=DP-2,3840x2160,0x0,1
#+end_src

** Config
#+begin_src conf :tangle "~/.config/hypr/hyprland.conf"
  $terminal = uwsm app -- foot -e tmux
  $menu = uwsm app -- $HOME/.local/bin/mybemenu
  $web = uwsm app -- zen-browser
  $editor = uwsm app -- emacs

  env = HYPRCURSOR_THEME,Posy_Cursor_Black
  env = HYPRCURSOR_SIZE,32
  env = XCURSOR_THEME,Posy_Cursor_Black
  env = XCURSOR_SIZE,32

  exec-once = systemctl --user start hyprpolkitagent
  exec-once = uwsm app -- qs -c noctalia-shell --no-duplicate #waybar #ashell
  # exec-once = uwsm app -- hyprpaper
  exec-once = uwsm app -- foot -T Scratchpad -e tmux
  # exec-once = uwsm app -- mako

  general {
      gaps_in = 5
      gaps_out = 10
      border_size = 2
      col.active_border = rgb(b6a0ff)
      col.inactive_border = rgb(000000)
      resize_on_border = false
      allow_tearing = true
      layout = master
  }

  cursor {
      no_hardware_cursors = false
      min_refresh_rate = 240
      no_warps = false 
  }

  render {
      new_render_scheduling = 1 
      direct_scanout = 1
  }

  decoration {
      rounding = 20
      rounding_power = 2
      
      active_opacity = 1.0
      inactive_opacity = 1.0
      
      shadow {
          enabled = true
          range = 4
          render_power = 3
          color = rgba(1a1a1aee)
      }
      
      blur {
          enabled = true
          size = 3
          passes = 2
          vibrancy = 0.1696
      }
  }

  animations {
      enabled = no

      bezier = easeOutQuint,0.23,1,0.32,1
      bezier = easeInOutCubic,0.65,0.05,0.36,1
      bezier = linear,0,0,1,1
      bezier = almostLinear,0.5,0.5,0.75,1.0
      bezier = quick,0.15,0,0.1,1

      animation = global, 1, 10, default
      animation = border, 1, 5.39, easeOutQuint
      animation = windows, 1, 4.79, easeOutQuint
      animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
      animation = windowsOut, 1, 1.49, linear, popin 87%
      animation = fadeIn, 1, 1.73, almostLinear
      animation = fadeOut, 1, 1.46, almostLinear
      animation = fade, 1, 3.03, quick
      animation = layers, 1, 3.81, easeOutQuint
      animation = layersIn, 1, 4, easeOutQuint, fade
      animation = layersOut, 1, 1.5, linear, fade
      animation = fadeLayersIn, 1, 1.79, almostLinear
      animation = fadeLayersOut, 1, 1.39, almostLinear
      animation = workspaces, 1, 1.94, almostLinear, fade
      animation = workspacesIn, 1, 1.21, almostLinear, fade
      animation = workspacesOut, 1, 1.94, almostLinear, fade
  }

  master {
      new_status = master
  }

  misc {
      disable_hyprland_logo = true
      disable_splash_rendering = true
      vfr = true
  }


  #############
  ### INPUT ###
  #############

  # https://wiki.hypr.land/Configuring/Variables/#input
  input {
      kb_layout = us
      kb_variant =
      kb_model =
      kb_options =
      kb_rules =

      follow_mouse = 1

      sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
      accel_profile = flat

      touchpad {
          natural_scroll = true
  	disable_while_typing = false
      }
  }

  ##############
  ### Groups ###
  ##############

  group {
      col.border_active = rgb(b6a0ff)
      col.border_inactive = rgb(000000)

      groupbar {
          gradients = false
  	render_titles = 1
          font_family = IosevkaTerm Nerd Font
          font_size = 13
  	font_weight_active = Semibold
  	font_weight_inactive = Semibold
          text_color = rgb(000000)
  	text_color_inactive = rgb(ffffff)
  	col.active = rgb(b6a0ff)
          col.inactive = rgb(000000)
      }
  }

  ###################
  ### KEYBINDINGS ###
  ###################

  $mainMod = SUPER
  $ipc = qs -c noctalia-shell ipc call

  bind = $mainMod, return, exec, $terminal
  bind = $mainMod SHIFT, W, exec, $web
  bind = $mainMod SHIFT, E, exec, $editor
  bind = $mainMod SHIFT, Q, killactive,
  bind = $mainMod SHIFT, escape, exit,
  bind = $mainMod, escape, exec, hyprctl reload
  bind = $mainMod SHIFT, space, togglefloating,
  bind = $mainMod, D, exec, $ipc launcher toggle
  bind = $mainMod SHIFT, Y, exec, $ipc controlCenter toggle
  bind = $mainMod SHIFT, U, exec, $ipc settings toggle
  bind = $mainMod, backspace, focusmonitor, +1

  bind = $mainMod SHIFT, S, exec, grim -g "$(slurp -d)" 

  bind = $mainMod, B, layoutmsg, cycleprev
  bind = $mainMod, F, layoutmsg, cyclenext
  bind = $mainMod, J, layoutmsg, swapwithmaster master
  binde = $mainMod CONTROL, B, layoutmsg, mfact -0.01
  binde = $mainMod CONTROL, F, layoutmsg, mfact +0.01
  bind = $mainMod, M, fullscreen, toggle

  bind = $mainMod, G, togglegroup
  bind = $mainMod, Tab, changegroupactive, f
  bind = $mainMod SHIFT, Tab, changegroupactive, b
  bind = $mainMod ALT, B, moveintogroup, l
  bind = $mainMod ALT, F, moveintogroup, r
  bind = $mainMod ALT, P, moveintogroup, u
  bind = $mainMod ALT, N, moveintogroup, d
  bind = $mainMod, comma, moveoutofgroup

  bind = $mainMod, 1, workspace, 1
  bind = $mainMod, 2, workspace, 2
  bind = $mainMod, 3, workspace, 3
  bind = $mainMod, 4, workspace, 4
  bind = $mainMod, 5, workspace, 5
  bind = $mainMod, 6, workspace, 6
  bind = $mainMod, 7, workspace, 7
  bind = $mainMod, 8, workspace, 8
  bind = $mainMod, 9, workspace, 9
  bind = $mainMod, 0, workspace, 10

  # Move active window to a workspace with mainMod + SHIFT + [0-9]
  bind = $mainMod SHIFT, 1, movetoworkspace, 1
  bind = $mainMod SHIFT, 2, movetoworkspace, 2
  bind = $mainMod SHIFT, 3, movetoworkspace, 3
  bind = $mainMod SHIFT, 4, movetoworkspace, 4
  bind = $mainMod SHIFT, 5, movetoworkspace, 5
  bind = $mainMod SHIFT, 6, movetoworkspace, 6
  bind = $mainMod SHIFT, 7, movetoworkspace, 7
  bind = $mainMod SHIFT, 8, movetoworkspace, 8
  bind = $mainMod SHIFT, 9, movetoworkspace, 9
  bind = $mainMod SHIFT, 0, movetoworkspace, 10

  # Example special workspace (scratchpad)
  bind = $mainMod, minus, togglespecialworkspace, magic
  bind = $mainMod SHIFT, minus, movetoworkspace, special:magic

  # Scroll through existing workspaces with mainMod + scroll
  bind = $mainMod, mouse_down, workspace, e+1
  bind = $mainMod, mouse_up, workspace, e-1

  # Move/resize windows with mainMod + LMB/RMB and dragging
  bindm = $mainMod, mouse:272, movewindow
  bindm = $mainMod, mouse:273, resizewindow

  # Laptop multimedia keys for volume and LCD brightness
  bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
  bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
  bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
  bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
  bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
  bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-
  bindel = $mainMod, XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 100%
  bindel = $mainMod, XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 1%

  # Requires playerctl
  bindel = , XF86AudioRaiseVolume, exec, $ipc volume increase
  bindel = , XF86AudioLowerVolume, exec, $ipc volume decrease
  bindl = , XF86AudioMute, exec, $ipc volume muteOutput
  bindel = , XF86MonBrightnessUp, exec, $ipc brightness increase
  bindel = , XF86MonBrightnessDown, exec, $ipc brightness decrease
  # bindl = , XF86AudioNext, exec, playerctl next
  # bindl = , XF86AudioPause, exec, playerctl play-pause
  # bindl = , XF86AudioPlay, exec, playerctl play-pause
  # bindl = , XF86AudioPrev, exec, playerctl previous

  ##############################
  ### WINDOWS AND WORKSPACES ###
  ##############################

  # See https://wiki.hypr.land/Configuring/Window-Rules/ for more
  # See https://wiki.hypr.land/Configuring/Workspace-Rules/ for workspace rules

  windowrule {
    name = float-scratchpad
    match:title = .*Scratchpad.*
    float = on
    size = (monitor_w*0.5) (monitor_h*0.5)
    move = (monitor_w*0.25) (monitor_h*0.25)
    workspace = special:magic
  }

  windowrule {
    name = gamescope
    match:class = ^(steam_app_.*|cs2|gamescope)$
    float = off
    fullscreen = on
    monitor = DP-1
  }

  layerrule {
    name = noctalia
    match:namespace = noctalia-background-.*$
    ignore_alpha = 0.5
    blur = true
    blur_popups = true
  }

  #windowrulev2 = float, class:^(Houdini FX)$, title:^(Save|Open|Preferences|Render|Export|Import)
  #windowrulev2 = tile, class:^(Houdini FX)$, title:.*Houdini.*
  #windowrulev2 = pseudo, class:^(Houdini FX)$, title:.*\.hip.*


  # windowrule = suppressevent maximize, class:.*
  # windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

  # windowrulev2 = float,title:^(Scratchpad)$
  # windowrulev2 = size 50% 50%,title:^(Scratchpad)$
  # windowrulev2 = center,title:^(Scratchpad)$
  # windowrulev2 = movetoworkspace,special:magic,title:^(Scratchgpad)$

  # windowrulev2 = noborder, class:appphotos
  # windowrulev2 = float,class:appphotos

#+end_src

* Hyprlauncher
#+begin_src conf :tangle "~/.config/hypr/hyprlauncher.conf"
  general {
    grab_focus = true
  }

  cache {
    enabled = true
  }

  finders {
    default_finder = desktop
    desktop_prefix =
    desktop_launch_prefix = uwsm app --
    desktop_icons = false
  }

  ui {
    window_size = 720 250
  }
#+end_src

* Hyprtoolkit
#+begin_src conf :tangle no :tgle "~/.config/hypr/hyprtoolkit.conf"
  background = 0xFF282828
  base = 0xFF1d2021
  text = 0xFFebdbb2
  alternate_base = 0xFF3c3836
  bright_text = 0xFFfbf1c7
  accent = 0xFF8ec07c
  accent_secondary = 0xFF83a598
  h1_size = 19
  h2_size = 15
  h3_size = 13
  font_size = 11
  small_font_size = 10
  font_family = IosevkaTerm Nerd Font
  font_family_monospace = IosevkaTerm Nerd Font
  icon_theme = Flatery-Dark
#+end_src

* Bemenu
#+begin_src bash :tangle "~/.local/bin/mybemenu" :tangle-mode (identity #o777) :mkdirp yes
  #!/bin/bash
  bemenu-run --fn "CommitMono Nerd Font 12" --fb "#1e1e2e" --ff "#cdd6f4" --nb "#1e1e2e" --nf "#cdd6f4" --tb "#1e1e2e" --hb "#1e1e2e" --tf "#f38ba8" --hf "#f9e2af" --af "#cdd6f4" --ab "#1e1e2e"
#+end_src
