#+TITLE: Hyprland Configuration
#+AUTHOR: Richard Redcroft
#+EMAIL: Richard@Redcroft.tech
#+OPTIONS: toc:nil num:nil
#+PROPERTY: Header-args :tangle-mode (identity #o444) :mkdirp yes
#+auto_tangle: t

* Hyprland
** Config
#+begin_src lua :tangle "~/.config/hypr/hyprland.lua" :tangle-mode (identity #o775)
  --------------------------------------------------
  -- HOSTNAME
  --------------------------------------------------

  local function get_hostname()
     local handle = io.popen("hostname")

     if not handle then
        return "unknown"
     end

     local result = handle:read("*l")
     handle:close()

     return result or "unknown"
  end

  local hostname = get_hostname()


  --------------------------------------------------
  -- HELPERS
  --------------------------------------------------

  local function monitor(config)
     hl.monitor(config)
  end

  local function exec(cmd)
     hl.exec_cmd(cmd)
  end


  --------------------------------------------------
  -- MACHINE CONFIGURATION
  --------------------------------------------------

  local machines = {
     heimdall = {
        monitors = function()
           monitor({
  	       output   = "DP-1",
  	       mode     = "3440x1440@175",
  	       position = "0x0",
  	       scale    = 1,
  	       bitdepth = 10,
  	       vrr      = 1,
  	       cm       = "auto",
           })
        end,

        startup = function()
           exec([[uwsm app -- openrgb --profile "off.orp"]])
        end,
     },

     red = {
        monitors = function()
           monitor({
  	       output   = "eDP-1",
  	       mode     = "1920x1080",
  	       position = "0x0",
  	       scale    = 1,
           })
        end,
     },

     poseidon = {
        monitors = function()
           monitor({
  	       output   = "DP-1",
  	       mode     = "3840x2160",
  	       position = "3840x0",
  	       scale    = 1,
           })

           monitor({
  	       output   = "DP-2",
  	       mode     = "3840x2160",
  	       position = "0x0",
  	       scale    = 1,
           })
        end,
     },

     default = {
        monitors = function()
           monitor({
  	       output   = "*",
  	       mode     = "preferred",
  	       position = "auto",
  	       scale    = 1,
           })
        end,
     },
  }

  local current_machine = machines[hostname] or machines.default


  --------------------------------------------------
  -- MONITORS
  --------------------------------------------------

  current_machine.monitors()


  --------------------------------------------------
  -- USER PROGRAMS
  --------------------------------------------------

  local apps = {
     terminal = "uwsm app -- ghostty",
     menu     = "uwsm app -- $HOME/.local/bin/mybemenu",
     browser  = "uwsm app -- zen-browser",
     editor   = "uwsm app -- emacs",
  }


  --------------------------------------------------
  -- ENVIRONMENT VARIABLES
  --------------------------------------------------

  local env = {
     DXVK_HDR             = 1,
     HYPRCURSOR_SIZE      = "32",
     HYPRCURSOR_THEME     = "Posy_Cursor_Black",
     PROTON_ENABLE_WAYLAND = 1,
     PROTON_ENABLE_HDR     = 1,
     PROTON_DLSS_UPGRADE   = 1,
     XCURSOR_SIZE         = "32",
     XCURSOR_THEME        = "Posy_Cursor_Black",
  }

  for key, value in pairs(env) do
     hl.env(key, value)
  end


  --------------------------------------------------
  -- AUTOSTART
  --------------------------------------------------

  hl.on("hyprland.start", function()
  	 local startup_commands = {
  	    "systemctl --user start hyprpolkitagent",
  	    "uwsm app -- waybar",
  	    [[uwsm app -- gsettings set org.gnome.desktop.interface color-scheme "prefer-dark"]],
  	    "uwsm app -- mako",
  	    "uwsm app -- udiskie",
  	 }

  	 for _, cmd in ipairs(startup_commands) do
  	    exec(cmd)
  	 end

  	 if current_machine.startup then
  	    current_machine.startup()
  	 end
  end)


  --------------------------------------------------
  -- LOOK AND FEEL
  --------------------------------------------------
  hl.config({
        animations = {
  	 enabled                  = false,
        },
        
        cursor = {
  	 no_hardware_cursors      = false,
  	 min_refresh_rate         = 60,
  	 no_warps                 = false,
        },

        decoration = {
  	 rounding                 = 12,
  	 rounding_power           = 2,

  	 active_opacity           = 1.0,
  	 inactive_opacity         = 1.0,

  	 shadow = {
  	    enabled               = false,
  	    range                 = 4,
  	    render_power          = 3,
  	    color                 = 0x441a1a1a,
  	 },

  	 blur = {
  	    enabled               = false,
  	    size                  = 10,
  	    passes                = 1,
  	    vibrancy              = 0.1696,
  	 }
        },

        dwindle = {
  	 preserve_split           = true,
        },
        
        general = {
  	 gaps_in                  = 4,
  	 gaps_out                 = 20,
  	 border_size              = 2,
  	 
  	 col = {
  	    active_border         = "rgb(484848)",
  	    inactive_border       = "rgb(000000)",
  	 },

  	 resize_on_border         = true,
  	 allow_tearing            = true,
        },

        input = {
  	 kb_layout                = "us",
  	 kb_variant               = "",
  	 kb_model                 = "",
  	 kb_options               = "",
  	 kb_rules                 = "",
  	 follow_mouse             = 1,
  	 sensitivity              = 0,
  	 accel_profile            = "flat",
  	 touchpad = {
  	    natural_scroll        = true,
  	    disable_while_typing  = false,
  	 },
        },

        master = {
  	new_status                = "master", 
        },

        misc = {
  	 background_color         = 0x000000,
  	 disable_hyprland_logo    = true,
  	 disable_splash_rendering = true,
        },

        render = {
  	 cm_auto_hdr              = 1,
  	 direct_scanout           = 1,
  	 new_render_scheduling    = 1,
        }

  })


  --------------------------------------------------
  -- ANIMATIONS
  --------------------------------------------------

  hl.curve("easeOutQuint",   { type = "bezier", points = { {0.23, 1},    {0.32, 1}    } })
  hl.curve("easeInOutCubic", { type = "bezier", points = { {0.65, 0.05}, {0.36, 1}    } })
  hl.curve("linear",         { type = "bezier", points = { {0, 0},       {1, 1}       } })
  hl.curve("almostLinear",   { type = "bezier", points = { {0.5, 0.5},   {0.75, 1}    } })
  hl.curve("quick",          { type = "bezier", points = { {0.15, 0},    {0.1, 1}     } })

  -- Default springs
  hl.curve("easy",           { type = "spring", mass = 1, stiffness = 71.2633, dampening = 15.8273644 })

  hl.animation({ leaf = "global",        enabled = true,  speed = 10,   bezier = "default" })
  hl.animation({ leaf = "border",        enabled = true,  speed = 5.39, bezier = "easeOutQuint" })
  hl.animation({ leaf = "windows",       enabled = true,  speed = 4.79, spring = "easy" })
  hl.animation({ leaf = "windowsIn",     enabled = true,  speed = 4.1,  spring = "easy",         style = "popin 87%" })
  hl.animation({ leaf = "windowsOut",    enabled = true,  speed = 1.49, bezier = "linear",       style = "popin 87%" })
  hl.animation({ leaf = "fadeIn",        enabled = true,  speed = 1.73, bezier = "almostLinear" })
  hl.animation({ leaf = "fadeOut",       enabled = true,  speed = 1.46, bezier = "almostLinear" })
  hl.animation({ leaf = "fade",          enabled = true,  speed = 3.03, bezier = "quick" })
  hl.animation({ leaf = "layers",        enabled = true,  speed = 3.81, bezier = "easeOutQuint" })
  hl.animation({ leaf = "layersIn",      enabled = true,  speed = 4,    bezier = "easeOutQuint", style = "fade" })
  hl.animation({ leaf = "layersOut",     enabled = true,  speed = 1.5,  bezier = "linear",       style = "fade" })
  hl.animation({ leaf = "fadeLayersIn",  enabled = true,  speed = 1.79, bezier = "almostLinear" })
  hl.animation({ leaf = "fadeLayersOut", enabled = true,  speed = 1.39, bezier = "almostLinear" })
  hl.animation({ leaf = "workspaces",    enabled = true,  speed = 1.94, bezier = "almostLinear", style = "fade" })
  hl.animation({ leaf = "workspacesIn",  enabled = true,  speed = 1.21, bezier = "almostLinear", style = "fade" })
  hl.animation({ leaf = "workspacesOut", enabled = true,  speed = 1.94, bezier = "almostLinear", style = "fade" })
  hl.animation({ leaf = "zoomFactor",    enabled = true,  speed = 7,    bezier = "quick" })


  --------------------------------------------------
  -- KEYBINDS
  --------------------------------------------------

  local mainMod = "SUPER"

  local mainMod = "SUPER"

  --------------------------------------------------
  -- GENERAL BINDS
  --------------------------------------------------
  hl.bind(mainMod .. " + return", hl.dsp.exec_cmd(apps.terminal))
  hl.bind(mainMod .. " + SHIFT + W", hl.dsp.exec_cmd(apps.browser))
  hl.bind(mainMod .. " + SHIFT + E", hl.dsp.exec_cmd(apps.editor))
  hl.bind(mainMod .. " + SHIFT + Q", hl.dsp.window.close())
  hl.bind(mainMod .. " + SHIFT + escape", hl.dsp.exit())
  hl.bind(mainMod .. " + escape", hl.dsp.exec_cmd("hyprctl reload"))
  hl.bind(mainMod .. " + SHIFT + space", hl.dsp.window.float({ action = "toggle" }))
  hl.bind(mainMod .. " + D", hl.dsp.exec_cmd(apps.menu))
  hl.bind(mainMod .. " + backspace", hl.dsp.focus({ monitor = "+1" }))
  hl.bind(mainMod .. " + Y", hl.dsp.exec_cmd("killall -SIGUSR1 waybar"))
  hl.bind(mainMod .. " + L", hl.dsp.exec_cmd("hyprlock"))

  -- Screenshot & Picker
  hl.bind(mainMod .. " + SHIFT + S", hl.dsp.exec_cmd([[grim -g "$(slurp -d)"]]))
  hl.bind(mainMod .. " + SHIFT + P", hl.dsp.exec_cmd("hyprpicker -a"))

  --------------------------------------------------
  -- NAVIGATION (Emacs/Vim-style focus)
  --------------------------------------------------
  hl.bind(mainMod .. " + F", hl.dsp.focus({ direction = "right" }))
  hl.bind(mainMod .. " + B", hl.dsp.focus({ direction = "left" }))
  hl.bind(mainMod .. " + P", hl.dsp.focus({ direction = "up" }))
  hl.bind(mainMod .. " + N", hl.dsp.focus({ direction = "down" }))
  hl.bind(mainMod .. " + M", hl.dsp.window.fullscreen({ action = "toggle" }))

  -- Resizing (Repeating)
  hl.bind(mainMod .. " + CONTROL + F", hl.dsp.window.resize({ x = 10, y = 0, relative = true }),  { repeating = true })
  hl.bind(mainMod .. " + CONTROL + B", hl.dsp.window.resize({ x = -10, y = 0, relative = true }), { repeating = true })
  hl.bind(mainMod .. " + CONTROL + P", hl.dsp.window.resize({ x = 0, y = -10, relative = true }), { repeating = true })
  hl.bind(mainMod .. " + CONTROL + N", hl.dsp.window.resize({ x = 0, y = 10, relative = true }),  { repeating = true })

  --------------------------------------------------
  -- GROUPS
  --------------------------------------------------
  hl.bind(mainMod .. " + G", hl.dsp.layout("togglegroup"))
  hl.bind(mainMod .. " + Tab", hl.dsp.layout("changegroupactive", "f"))
  hl.bind(mainMod .. " + SHIFT + Tab", hl.dsp.layout("changegroupactive", "b"))
  hl.bind(mainMod .. " + ALT + B", hl.dsp.layout("moveintogroup", "l"))
  hl.bind(mainMod .. " + ALT + F", hl.dsp.layout("moveintogroup", "r"))
  hl.bind(mainMod .. " + ALT + P", hl.dsp.layout("moveintogroup", "u"))
  hl.bind(mainMod .. " + ALT + N", hl.dsp.layout("moveintogroup", "d"))
  hl.bind(mainMod .. " + comma", hl.dsp.layout("moveoutofgroup"))

  --------------------------------------------------
  -- WORKSPACES
  --------------------------------------------------
  for i = 1, 10 do
      local key = i % 10
      hl.bind(mainMod .. " + " .. key, hl.dsp.focus({ workspace = i }))
      hl.bind(mainMod .. " + SHIFT + " .. key, hl.dsp.window.move({ workspace = i }))
  end

  -- Special Workspace
  hl.bind(mainMod .. " + minus", hl.dsp.workspace.toggle_special("magic"))
  hl.bind(mainMod .. " + SHIFT + minus", hl.dsp.window.move({ workspace = "special:magic" }))

  --------------------------------------------------
  -- MOUSE
  --------------------------------------------------
  hl.bind(mainMod .. " + mouse_down", hl.dsp.focus({ workspace = "e+1" }))
  hl.bind(mainMod .. " + mouse_up",   hl.dsp.focus({ workspace = "e-1" }))
  hl.bind(mainMod .. " + mouse:272", hl.dsp.window.drag(),   { mouse = true })
  hl.bind(mainMod .. " + mouse:273", hl.dsp.window.resize(), { mouse = true })

  --------------------------------------------------
  -- MULTIMEDIA & BRIGHTNESS
  --------------------------------------------------
  function show_osd(message, icon_name)
      local icon_path = "/usr/share/icons/Adwaita/symbolic/status/" .. icon_name .. ".svg"
      
      local f = io.open(icon_path, "r")
      if f then 
          f:close() 
      else
          icon_path = "/usr/share/icons/AdwaitaLegacy/32x32/legacy/" .. icon_name .. ".png"
      end

      hl.exec_cmd("makoctl dismiss -a 'osd'")
      
      local gdbus_cmd = string.format(
          [[gdbus call --session --dest org.freedesktop.Notifications ]] ..
          [[--object-path /org/freedesktop/Notifications ]] ..
          [[--method org.freedesktop.Notifications.Notify ]] ..
          [["osd" 999 "%s" "%s" "" "[]" ]] ..
          [[ '{"x-mako-stack-tag": <"osd-notification">, "transient": <true>}' 500]],
          icon_path, message
      )

      hl.exec_cmd(gdbus_cmd)
  end

  local function monitor_cmd(cmd_type, action, value)
     local monitor_call = string.format("monitorctl %s %s %s", cmd_type, action, value or "")
     local osd_logic = [[hyprctl lua 'show_osd("Brightness $(monitorctl b -g)%%", "display-brightness-symbolic")']]
     return hl.dsp.exec_cmd(monitor_call .. " && " .. osd_logic)
  end

  -- Audio (unchanged, but could be moved to monitorctl v if preferred)
  hl.bind("XF86AudioRaiseVolume", hl.dsp.exec_cmd("wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+"), { locked = true, repeating = true })
  hl.bind("XF86AudioLowerVolume", hl.dsp.exec_cmd("wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-"),   { locked = true, repeating = true })
  hl.bind("XF86AudioMute",        hl.dsp.exec_cmd("wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle"), { locked = true })

  -- Brightness Binds using monitorctl
  hl.bind("XF86MonBrightnessUp",   monitor_cmd("b", "-i", "5"), { locked = true, repeating = true })
  hl.bind("XF86MonBrightnessDown", monitor_cmd("b", "-d", "5"), { locked = true, repeating = true })

  -- Preset Levels
  hl.bind(mainMod .. " + XF86MonBrightnessUp",   monitor_cmd("b", "", "30"),  { locked = true })
  hl.bind(mainMod .. " + XF86MonBrightnessDown", monitor_cmd("b", "", "5"),   { locked = true })
  hl.bind(mainMod .. " + SHIFT + XF86MonBrightnessUp", monitor_cmd("b", "", "100"), { locked = true })

  --------------------------------------------------
  -- WINDOW RULES
  --------------------------------------------------

  -- Suppress Maximize (Global)
  hl.window_rule({
      name  = "suppress-maximize",
      match = { class = ".*" },
      suppress_event = "maximize",
  })

  -- Scratchpad Rule
  hl.window_rule({
      name = "float-scratchpad",
      match = { title = ".*Scratchpad.*" },
      float = true,
      size = {"(monitor_w*0.6)", "(monitor_h*0.6)"},
      move = {"(monitor_w*0.15)", "(monitor_h*0.15)"},
      workspace = "special:magic"
  })

  -- Unreal Engine Fixes
  hl.window_rule({
      name = "unreal-engine-tweak",
      match = { class = "UnrealEditor", title = "%w*" },
      no_initial_focus = true,
      suppress_event = "activate"
  })

  -- Video/Photo Editing Rules (Resolve & Affinity)
  local editing_apps = {
      { name = "davinci", class = "resolve", title = "resolve" },
      { name = "affinity", class = "affinity.exe", title = "" }
  }

  for _, app in ipairs(editing_apps) do
      hl.window_rule({
          name = app.name .. "-tweak",
          match = { class = app.class, title = app.title },
          no_blur = true,
          border_size = 0,
          no_shadow = true
      })
  end

  -- Affinity Tile Override
  hl.window_rule({
      name = "affinity-photo-tile-tweak",
      match = { class = "affinity.exe", title = "Affinity" },
      tile = true
  })

  -- Gamescope / Gaming Rule
  hl.window_rule({
      name = "gamescope",
      match = { class = "^(steam_app_.*|cs2|gamescope)$" },
      float = false,
      fullscreen = true,
      monitor = "DP-1" -- Note: On 'poseidon' this works, on 'red' you might want to use 0 or current
  })

  -- Simple Floats
  hl.window_rule({
      name = "floating-windows",
      match = { title = "Steam - Self Updater" },
      float = true
  })
#+end_src
* Hyprlauncher
#+begin_src conf :tangle "~/.config/hypr/hyprlauncher.conf"
  general {
    grab_focus = true
  }

  cache {
    enabled = true
  }

  finders {
    default_finder = desktop
    desktop_prefix =
    desktop_launch_prefix = uwsm app --
    desktop_icons = false
  }

  ui {
    window_size = 720 250
  }
#+end_src

* Hypridle
#+begin_src conf :tangle "~/.config/hypr/hypridle.conf"
  general {
      lock_cmd = pidof hyprlock || hyprlock
      before_sleep_cmd = loginctl lock-session
      after_sleep_cmd = hyprctl dispatch dpms on
  }

  # Dim screen after 1 min
  listener {
      timeout = 60
      on-timeout = brightnessctl -s set 10
      on-resume = brightnessctl -r
  }

  # Monitor DPMS off after 3 mins
  listener {
      timeout = 180
      on-timeout = hyprctl dispatch dpms off
      on-resume = hyprctl dispatch dpms on
  }

  # This is the "Safety Net" for manual locks
  # It ensures that if the session is locked, we listen for resume events
  listener {
      timeout = 300
      on-timeout = loginctl lock-session
  }
#+end_src

* Hyprlock
#+begin_src conf :tangle "~/.config/hypr/hyprlock.conf"
  general {
    hide_cursor = true
    on_unlock = hyprctl dispatch dpms on
  }

  input-field {
    monitor =
    size = 20%, 5%
    outline_thickness = 3
    inner_color = rgba(0, 0, 0, 0.0)

    outer_color = rgba(33ccffee)
    check_color = rgba(00ff99ee)
    fail_color = rgba(ff66ffee)

    font_color = rgb(143, 143, 143)
    fade_on_empty = true
    rounding = 12

    font_family = JetBrainsMono Nerd Font
    placeholder_text = Input Password...
    fail_text = $PAMFAIL

    dots_spacing = 0.4

    halign = center
    valign = center
  }
#+end_src

* Hyprtoolkit
#+begin_src conf :tangle "~/.config/hypr/hyprtoolkit.conf"
  background = 0xFF282828
  base = 0xFF1d2021
  text = 0xFFebdbb2
  alternate_base = 0xFF3c3836
  bright_text = 0xFFfbf1c7
  accent = 0xFF8ec07c
  accent_secondary = 0xFF83a598
  h1_size = 19
  h2_size = 15
  h3_size = 13v
  font_size = 11
  small_font_size = 10
  font_family = Ioskeley Mono
  font_family_monospace = Ioskeley Mono
  icon_theme = Flatery-Dark
#+end_src

* Bemenu
#+begin_src bash :tangle "~/.local/bin/mybemenu" :tangle-mode (identity #o777) :mkdirp yes
  #!/bin/bash
  bemenu-run --fn "Ioskeley Mono 12" --fb "#000000" --ff "#cdd6f4" --nb "#1e1e2e" --nf "#cdd6f4" --tb "#1e1e2e" --hb "#1e1e2e" --tf "#f38ba8" --hf "#f9e2af" --af "#cdd6f4" --ab "#1e1e2e"
#+end_src
