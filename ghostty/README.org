#+TITLE: Ghostty Terminal
#+AUTHOR: Richard Redcroft
#+EMAIL: Richard@Redcroft.tech
#+OPTIONS: toc:nil num:nil
#+PROPERTY: Header-args :tangle ~/.config/ghostty/config :tangle-mode (identity #o444) :mkdirp yes :noweb yes
#+auto_tangle: t

#+INCLUDE: "../theme.org"

* Theme Helpers :noexport:
#+NAME: get-val
#+begin_src emacs-lisp :var key="font-family" :exports none :tangle no
  (with-current-buffer (find-file-noselect "../theme.org")
    (save-excursion
      (goto-char (point-min))
      (re-search-forward "^#\\+NAME: theme-table")
      (forward-line)
      (let* ((full-table (org-babel-read-table))
             (headers (car full-table))
             (table (cl-remove-if (lambda (x) (or (symbolp x) (not (listp x)))) (cdr full-table)))
             (host (system-name))
             (col-idx (or (cl-position host headers :test 'string-equal) 1))
             (row (assoc key table))
             (val (nth col-idx row))
             (val-str (format "%s" val)))
        (if (or (null val) (string-equal val-str "") (string-equal val-str "-") (string-equal val-str "nil"))
            (nth 1 row)
          val))))
#+end_src

#+NAME: get-color
#+begin_src emacs-lisp :var name="bg" :exports none :tangle no
  (with-current-buffer (find-file-noselect "../theme.org")
    (save-excursion
      (goto-char (point-min))
      (re-search-forward "^#\\+NAME: palette")
      (forward-line)
      (let* ((full-table (org-babel-read-table))
             (headers (car full-table))
             (table (cl-remove-if (lambda (x) (or (symbolp x) (not (listp x)))) (cdr full-table)))
             (host (system-name))
             (col-idx (or (cl-position host headers :test 'string-equal) 1))
             (row (assoc name table))
             (val (nth col-idx row))
             (val-str (format "%s" val)))
        (if (or (null val) (string-equal val-str "") (string-equal val-str "-") (string-equal val-str "nil"))
            (nth 1 row)
          val))))
#+end_src







* Config

#+begin_src conf
  font-family = "<<get-val("font-family")>>"
  font-size = <<get-val("font-size")>>
  font-style = "<<get-val("font-style")>>"
  quick-terminal-position = center
  quick-terminal-size = 60%,60%

  window-padding-x = 6
  window-padding-y = 6

  window-decoration = none
#+end_src

#+begin_src conf :tangle no
  initial-window = false
  quit-after-last-window-closed = false
  keybind = global:super+minus=toggle_quick_terminal
#+end_src

#+begin_src conf
  # Centralized Theme
  background = <<get-color("bg")>>
  foreground = <<get-color("fg")>>
  cursor-color = <<get-color("red")>>
  cursor-text = <<get-color("bg")>>
  selection-background = <<get-color("blue")>>
  selection-foreground = <<get-color("fg")>>

  # Normal colors
  palette = 0=<<get-color("bg")>>
  palette = 1=<<get-color("red")>>
  palette = 2=<<get-color("green")>>
  palette = 3=<<get-color("yellow")>>
  palette = 4=<<get-color("blue")>>
  palette = 5=<<get-color("magenta")>>
  palette = 6=<<get-color("cyan")>>
  palette = 7=<<get-color("fg")>>

  # Bright colors (same as normal)
  palette = 8=<<get-color("bg")>>
  palette = 9=<<get-color("red")>>
  palette = 10=<<get-color("green")>>
  palette = 11=<<get-color("yellow")>>
  palette = 12=<<get-color("blue")>>
  palette = 13=<<get-color("magenta")>>
  palette = 14=<<get-color("cyan")>>
  palette = 15=<<get-color("fg")>>
#+end_src
