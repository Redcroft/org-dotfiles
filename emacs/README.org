#+TITLE: Emacs Configuration
#+AUTHOR: Richard Redcroft
#+EMAIL: Richard@Redcroft.tech
#+OPTIONS: toc:nil num:nil
#+PROPERTY: Header-args :tangle ~/.emacs.d/init.el :tangle-mode (identity #o444) :mkdirp yes :noweb yes

* Early Init
** Visuals
#+begin_src emacs-lisp :tangle ~/.emacs.d/early-init.el
  ;;; early-init.el --- Early initialization  -*- lexical-binding: t; -*-

  (push '(menu-bar-lines . 0) default-frame-alist)
  (push '(tool-bar-lines . 0) default-frame-alist)
  (push '(vertical-scroll-bars nil . nil) default-frame-alist)
  (push '(background-color . "#000000") default-frame-alist)
  ;; PGTK creates the first frame after early-init. Set the font here so
  ;; that frame is born with it; set-face-attribute during init is too late
  ;; and gets wiped when the theme recalculates `default'.
  (push '(font . "Pragmasevka Nerd Font-13") default-frame-alist)

  (defvar default-file-name-handler-alist file-name-handler-alist)
  (setq file-name-handler-alist nil)

#+end_src
** GC
#+begin_src emacs-lisp :tangle ~/.emacs.d/early-init.el
  ;; early-init.el
  (setq gc-cons-threshold most-positive-fixnum
        file-name-handler-alist nil
        package-enable-at-startup nil)

  (add-hook 'emacs-startup-hook
            (lambda ()
              (setq gc-cons-threshold (* 16 1024 1024)
                    gc-cons-percentage 0.1
                    file-name-handler-alist default-file-name-handler-alist)))
#+end_src
** Native Comp
#+begin_src emacs-lisp :tangle ~/.emacs.d/early-init.el
  (when (native-comp-available-p)
    (setq native-comp-deferred-compilation t
          native-comp-jit-compilation t))
#+end_src
* Init
** Elpaca
#+begin_src emacs-lisp
  ;;; init.el --- Emacs initialization  -*- lexical-binding: t; -*-

  ;; ============================================================
  ;; Bootstrap Elpaca
  ;; ============================================================
  ;; Elpaca's built-in Emacs release table does not yet include 31.1
  ;; (2026-08-24), and this build has a nil emacs-build-time.
  (setq elpaca-core-date
        (list (or (and emacs-build-time
                       (string-to-number (format-time-string "%Y%m%d" emacs-build-time)))
                  20260824)))
  (defvar elpaca-installer-version 0.12)
  (defvar elpaca-directory (expand-file-name "elpaca/" user-emacs-directory))
  (defvar elpaca-builds-directory (expand-file-name "builds/" elpaca-directory))
  (defvar elpaca-sources-directory (expand-file-name "sources/" elpaca-directory))
  (defvar elpaca-order '(elpaca :repo "https://github.com/progfolio/elpaca.git"
                                :ref nil :depth 1 :inherit ignore
                                :files (:defaults "elpaca-test.el" (:exclude "extensions"))
                                :build (:not elpaca-activate)))
  (let* ((repo  (expand-file-name "elpaca/" elpaca-sources-directory))
         (build (expand-file-name "elpaca/" elpaca-builds-directory))
         (order (cdr elpaca-order))
         (default-directory repo))
    (add-to-list 'load-path (if (file-exists-p build) build repo))
    (unless (file-exists-p repo)
      (make-directory repo t)
      (when (<= emacs-major-version 28) (require 'subr-x))
      (condition-case-unless-debug err
          (if-let* ((buffer (pop-to-buffer-same-window "*elpaca-bootstrap*"))
                    ((zerop (apply #'call-process `("git" nil ,buffer t "clone"
                                                    ,@(when-let* ((depth (plist-get order :depth)))
                                                        (list (format "--depth=%d" depth) "--no-single-branch"))
                                                    ,(plist-get order :repo) ,repo))))
                    ((zerop (call-process "git" nil buffer t "checkout"
                                          (or (plist-get order :ref) "--"))))
                    (emacs (concat invocation-directory invocation-name))
                    ((zerop (call-process emacs nil buffer nil "-Q" "-L" "." "--batch"
                                          "--eval" "(byte-recompile-directory \".\" 0 'force)")))
                    ((require 'elpaca))
                    ((elpaca-generate-autoloads "elpaca" repo)))
              (progn (message "%s" (buffer-string)) (kill-buffer buffer))
            (error "%s" (with-current-buffer buffer (buffer-string))))
        ((error) (warn "%s" err) (delete-directory repo 'recursive))))
    (unless (require 'elpaca-autoloads nil t)
      (require 'elpaca)
      (elpaca-generate-autoloads "elpaca" repo)
      (let ((load-source-file-function nil)) (load "./elpaca-autoloads"))))
  (add-hook 'after-init-hook #'elpaca-process-queues)
  (elpaca `(,@elpaca-order))
#+end_src
** Use-package
#+begin_src emacs-lisp
  ;; ============================================================
  ;; use-package integration
  ;; ============================================================
  (elpaca elpaca-use-package
    (elpaca-use-package-mode)
    (setq use-package-always-ensure t))
#+end_src
** Core
#+begin_src emacs-lisp
  ;; ============================================================
  ;; Core / built-ins
  ;; ============================================================
  (use-package emacs
    :ensure nil
    :bind (("C-x r s" . replace-string)
  	 ("C-c r" . eval-buffer)
    	 ("M-#" . comment-or-uncomment-region))
    :hook
    (after-init . (lambda ()
                    (keyboard-translate ?\C-x ?\C-t)
                    (keyboard-translate ?\C-t ?\C-x)))
    :init
    (column-number-mode -1)
    (global-display-line-numbers-mode 1)
    (recentf-mode 1)
    (save-place-mode 1)
    (scroll-bar-mode -1)
    (dolist (mode '(term-mode shell-mode eshell-mode eww-mode org-mode))
      (add-hook (intern (concat (symbol-name mode) "-hook"))
                (lambda () (display-line-numbers-mode -1))))
    :config
    (setq auth-sources '("~/.authinfo.gpg"))
    (setq apropos-do-all t
    	custom-file (concat user-emacs-directory "custom.el")
    	custom-safe-themes t
    	inhibit-startup-screen t
    	use-short-answers t
  	display-line-numbers-type 'visual
  	recentf-max-saved-items 500
  	indent-tabs-mode nil
  	)
    (electric-indent-mode 1)
    (electric-pair-mode 1)
    )

  (use-package savehist
    :ensure nil
    :init (savehist-mode))


  (use-package which-key
    :ensure nil
    :init (which-key-mode)
    :custom (which-key-idle-delay 0.5))

  (use-package compat :ensure (:wait t))
#+end_src
** Gnus
#+begin_src emacs-lisp
  (use-package gnus
    :ensure nil
    :config
    ;; Recieve
    (setq gnus-select-method '(nnnil ""))
    (setq gnus-secondary-select-methods
          '((nnimap "gmail"
                    (nnimap-address "imap.gmail.com")
                    (nnimap-server-port 993)
                    (nnimap-stream ssl))

            (nnimap "work"
                    (nnimap-address "imap.gmail.com")
                    (nnimap-server-port 993)
                    (nnimap-stream ssl))

            (nnimap "redcroft"
                    (nnimap-address "imap.hostinger.com")
                    (nnimap-server-port 993)
                    (nnimap-stream ssl))))

    (setq gnus-parameters
  	'(("nnimap\\+gmail:.*"
             (nnimap-user "animationrich@gmail.com"))

            ("nnimap\\+work:.*"
             (nnimap-user "richard@carseandwaterman.com"))

            ("nnimap\\+redcroft:.*"
             (nnimap-user "richard@redcroft.tech"))))

    ;; Sending
    (setq message-send-mail-function
  	'smtpmail-send-it)
    (setq smtpmail-stream-type 'starttls)
    (setq smtpmail-smtp-service 587)
    (setq gnus-posting-styles
  	'(("nnimap\\+gmail:"
             (address "animationrich@gmail.com")
             (name "Richard Redcroft"))

            ("nnimap\\+work:"
             (address "richard@carseandwaterman.com")
             (name "Richard DW Redcroft"))

            ("nnimap\\+redcroft:"
             (address "richard@redcroft.tech")
             (name "Richard Redcroft"))))
    (setq gnus-use-cache t)
    (setq gnus-asynchronous t)
    (setq gnus-fetch-old-headers t)
    (setq gnus-save-newsrc-file nil)
    (setq gnus-read-active-file 'some)
    )
#+end_src

** Org
#+begin_src emacs-lisp
  ;; ============================================================
  ;; Org-Mode (Built-in)
  ;; ============================================================
  (use-package org
    :ensure nil
    :bind (("C-c x" . org-capture)
  	 ("C-c e" . org-publish-all))
    :custom
    (org-fold-core-style 'overlays)
    (org-src-window-setup 'current-window)
    (org-src-fontify-natively t)
    :custom-face
    (org-quote ((t (:slant italic)))))

  (use-package ob-shell :ensure nil)
  (use-package org-tempo :ensure nil)

  (use-package org-auto-tangle
    :hook (org-mode . org-auto-tangle-mode))

  ;;(use-package org-modern
  ;;  :after (org)
  ;;  :hook (org-mode . org-modern-mode))
#+end_src

** Kirigami
#+begin_src emacs-lisp
  (use-package hideshow
    :ensure nil ; Built-in to Emacs
    :hook (prog-mode . hs-minor-mode)
    :bind (:map hs-minor-mode-map
                ("C-c TAB" . hs-toggle-hiding)
                ("C-c S"   . hs-show-all)
  	      ("C-c H"   . hs-hide-all)))
#+end_src

** Ligatures
#+begin_src emacs-lisp
  (use-package ligature
    :config
    (ligature-set-ligatures 't '("www"))
    (ligature-set-ligatures 'eww-mode '("ff" "fi" "ffi"))
    (ligature-set-ligatures 'prog-mode
                          '(;; == === ==== => =| =>>=>=|=>==>> ==< =/=//=// =~
                            ;; =:= =!=
                            ("=" (rx (+ (or ">" "<" "|" "/" "~" ":" "!" "="))))
                            ;; ;; ;;;
                            (";" (rx (+ ";")))
                            ;; && &&&
                            ("&" (rx (+ "&")))
                            ;; !! !!! !. !: !!. != !== !~
                            ("!" (rx (+ (or "=" "!" "\." ":" "~"))))
                            ;; ?? ??? ?:  ?=  ?.
                            ("?" (rx (or ":" "=" "\." (+ "?"))))
                            ;; %% %%%
                            ("%" (rx (+ "%")))
                            ;; |> ||> |||> ||||> |] |} || ||| |-> ||-||
                            ;; |->>-||-<<-| |- |== ||=||
                            ;; |==>>==<<==<=>==//==/=!==:===>
                            ("|" (rx (+ (or ">" "<" "|" "/" ":" "!" "}" "\]"
                                            "-" "=" ))))
                            ;; \\ \\\ \/
                            ("\\" (rx (or "/" (+ "\\"))))
                            ;; ++ +++ ++++ +>
                            ("+" (rx (or ">" (+ "+"))))
                            ;; :: ::: :::: :> :< := :// ::=
                            (":" (rx (or ">" "<" "=" "//" ":=" (+ ":"))))
                            ;; // /// //// /\ /* /> /===:===!=//===>>==>==/
                            ("/" (rx (+ (or ">"  "<" "|" "/" "\\" "\*" ":" "!"
                                            "="))))
                            ;; .. ... .... .= .- .? ..= ..<
                            ("\." (rx (or "=" "-" "\?" "\.=" "\.<" (+ "\."))))
                            ;; -- --- ---- -~ -> ->> -| -|->-->>->--<<-|
                            ("-" (rx (+ (or ">" "<" "|" "~" "-"))))
                            ;; *> */ *)  ** *** ****
                            ("*" (rx (or ">" "/" ")" (+ "*"))))
                            ;; www wwww
                            ("w" (rx (+ "w")))
                            ;; <> <!-- <|> <: <~ <~> <~~ <+ <* <$ </  <+> <*>
                            ;; <$> </> <|  <||  <||| <|||| <- <-| <-<<-|-> <->>
                            ;; <<-> <= <=> <<==<<==>=|=>==/==//=!==:=>
                            ;; << <<< <<<<
                            ("<" (rx (+ (or "\+" "\*" "\$" "<" ">" ":" "~"  "!"
                                            "-"  "/" "|" "="))))
                            ;; >: >- >>- >--|-> >>-|-> >= >== >>== >=|=:=>>
                            ;; >> >>> >>>>
                            (">" (rx (+ (or ">" "<" "|" "/" ":" "=" "-"))))
                            ;; #: #= #! #( #? #[ #{ #_ #_( ## ### #####
                            ("#" (rx (or ":" "=" "!" "(" "\?" "\[" "{" "_(" "_"
                                         (+ "#"))))
                            ;; ~~ ~~~ ~=  ~-  ~@ ~> ~~>
                            ("~" (rx (or ">" "=" "-" "@" "~>" (+ "~"))))
                            ;; __ ___ ____ _|_ __|____|_
                            ("_" (rx (+ (or "_" "|"))))
                            ;; Fira code: 0xFF 0x12
                            ("0" (rx (and "x" (+ (in "A-F" "a-f" "0-9")))))
                            ;; Fira code:
                            "Fl"  "Tl"  "fi"  "fj"  "fl"  "ft"
                            ;; The few not covered by the regexps.
                            "{|"  "[|"  "]#"  "(*"  "}#"  "$>"  "^="))
    (global-ligature-mode t))
#+end_src

** Magit
#+begin_src emacs-lisp
  ;; ============================================================
  ;; Magit
  ;; ============================================================
  (use-package magit
    :defer t
    :bind ("C-x g" . magit-status)
    :custom (magit-diff-refine-hunk t))
#+end_src

** MC
#+begin_src emacs-lisp
  (use-package multiple-cursors
    :ensure t
    :bind (("C-c m c" . mc/edit-lines)
           ("C-c m n" . mc/mark-next-like-this)
           ("C-c m p" . mc/mark-previous-like-this)
           ("C-c m a" . mc/mark-all-like-this)
           ("C-c m r" . mc/mark-all-in-region)))
#+end_src

** Mise
#+begin_src emacs-lisp
  ;; ============================================================
  ;; Mise
  ;; ============================================================
  (use-package mise
    :ensure t
    :config
    (global-mise-mode 1)
    (setq mise-update-on-eshell-directory-change t)

    ;; Fix: Wrap the call so it passes the current buffer
    (add-hook 'find-file-hook (lambda () (interactive) (mise-update-buffer (current-buffer))))
    
    ;; Do the same for project switching if you use it
    (with-eval-after-load 'project
      (add-hook 'project-switch-hook (lambda () (mise-update-buffer (current-buffer))))))
#+end_src

** Move Text
#+begin_src emacs-lisp
  ;; ============================================================
  ;; Move Text
  ;; ============================================================
  (use-package move-text
    :init (move-text-default-bindings))
#+end_src

** No Littering
#+begin_src emacs-lisp
  ;; ============================================================
  ;; No Littering
  ;; ============================================================
  (use-package no-littering
    :custom
    ;; This handles the #auto-save# files (which you already have)
    (auto-save-file-name-transforms
     `((".*" ,(no-littering-expand-var-file-name "auto-save/") t)))
    
    ;; This handles the backup~ files
    (backup-directory-alist
     `((".*" . ,(no-littering-expand-var-file-name "backup/"))))

    (setq custom-file (no-littering-expand-etc-file-name "custom.el"))
    
    (delete-old-versions t)
    (kept-new-versions 6)
    (kept-old-versions 2)
    (version-control t))
#+end_src

** Project
#+begin_src emacs-lisp
  (use-package project
    :ensure nil
    :config
    (project-remember-projects-under "~/Projects")
    (defun project-remember-git-project ()
      (when-let ((project (project-current)))
        (when (eq (nth 1 project) 'Git)
          (project-remember-project project nil t))))
    (add-hook 'find-file-hook #'project-remember-git-project)
    (add-hook 'dired-mode-hook #'project-remember-git-project))
#+end_src

** Rainbow Mode
#+begin_src emacs-lisp
  (use-package rainbow-mode
    :hook ((org-mode     . rainbow-mode)
           (css-mode     . rainbow-mode)
           (css-ts-mode  . rainbow-mode)
           (html-mode    . rainbow-mode)
           (html-ts-mode . rainbow-mode)
           (mhtml-mode   . rainbow-mode)
           (nxml-mode    . rainbow-mode)
           (qml-ts-mode  . rainbow-mode)))
#+end_src

** Color Picker
#+begin_src emacs-lisp
  (use-package zenity-color-picker
    :bind ("C-x c" . zenity-cp-adjust-color-at-point))
#+end_src

** Search
#+begin_src emacs-lisp
  ;; ============================================================
  ;; Search and Navigation — Consult
  ;; ============================================================
  (use-package consult
    :bind (("C-s"   . consult-line)
           ("M-y"   . consult-yank-pop)
           ("C-x b" . consult-buffer)
           ("M-s r" . consult-ripgrep)
           ("M-g i" . consult-imenu)
           ("M-g I" . consult-imenu-multi))
    :init
    (setq register-preview-delay    0.5
          register-preview-function #'consult-register-format))
#+end_src

** Workspaces
#+begin_src emacs-lisp
  ;; ============================================================
  ;; Workspaces
  ;; ============================================================
  (use-package eyebrowse
    :bind (("M-1" . eyebrowse-switch-to-window-config-1)
           ("M-2" . eyebrowse-switch-to-window-config-2)
           ("M-3" . eyebrowse-switch-to-window-config-3)
           ("M-4" . eyebrowse-switch-to-window-config-4)
           ("M-5" . eyebrowse-switch-to-window-config-5)
           ("M-6" . eyebrowse-switch-to-window-config-6)
           ("M-7" . eyebrowse-switch-to-window-config-7)
           ("M-8" . eyebrowse-switch-to-window-config-8)
           ("M-9" . eyebrowse-switch-to-window-config-9)
           ("M-0" . eyebrowse-switch-to-window-config-0))
    :init (eyebrowse-mode)
    :custom
    (eyebrowse-new-workspace t))
#+end_src
** LSP
#+begin_src emacs-lisp
  ;; ============================================================
  ;; LSP via Eglot (built-in)
  ;; ============================================================
  (use-package eglot
    :ensure nil
    :hook ((c-ts-mode        . eglot-ensure)
           (c++-ts-mode      . eglot-ensure)
           (c-or-c++-ts-mode . eglot-ensure)
           (sh-base-mode     . eglot-ensure)
           (elixir-ts-mode   . eglot-ensure)
           (python-base-mode . eglot-ensure)
  	 (qml-ts-mode      . eglot-ensure)
           (js-base-mode     . eglot-ensure)
           (lua-ts-mode      . eglot-ensure)
           (rust-ts-mode     . eglot-ensure)
  	 (odin-ts-mode     . eglot-ensure)
  	 (zig-ts-mode      . eglot-ensure))
    :bind (:map eglot-mode-map
  	      ("C-c a" . eglot-code-actions)
                ("C-c r" . eglot-rename)
  	      ("C-c h" . eglot-inlay-hints-mode)
                ("C-c f" . eglot-format-buffer)
  	      ("C-c t" . eglot-find-typeDefinition))
    :custom
    (eldoc-idle-delay   0.1)
    (eglot-autoshutdown t))

  (use-package consult-eglot
    :after (consult eglot)
    :bind (:map eglot-mode-map
                ("M-?" . consult-eglot-symbols)))
#+end_src
** Completions
#+begin_src emacs-lisp
    ;; ============================================================
  ;; Completion UI — Vertico / Orderless / Marginalia
  ;; ============================================================
  (use-package vertico
    :init (vertico-mode))

  (use-package orderless
    :custom
    (completion-styles              '(orderless basic))
    (completion-category-defaults  nil)
    (completion-category-overrides '((file (styles partial-completion)))))

  (use-package marginalia
    :bind (:map minibuffer-local-map
                ("M-A" . marginalia-cycle))
    :init (marginalia-mode))

  ;; ============================================================
  ;; In-buffer Completion — Corfu
  ;; ============================================================
  (use-package corfu
    :custom
    (corfu-auto            t)
    (corfu-auto-prefix     2)
    (corfu-quit-at-boundary 'separator)
    :init (global-corfu-mode))

  (use-package nerd-icons-corfu
    :after corfu
    :init
    (add-to-list 'corfu-margin-formatters #'nerd-icons-corfu-formatter))

  (use-package cape
    :init
    (add-hook 'completion-at-point-functions #'cape-dabbrev)
    (add-hook 'completion-at-point-functions #'cape-file))

#+end_src
** Embark
#+begin_src emacs-lisp
  ;; ============================================================
  ;; Embark — contextual actions on any candidate
  ;; ============================================================
  (use-package embark
    :bind (("C-." . embark-act)
           ("C-;" . embark-dwim))
    :init
    ;; Show embark actions instead of the default prefix help
    (setq prefix-help-command #'embark-prefix-help-command))

  (use-package embark-consult
    :after (embark consult)
    :hook (embark-collect-mode . consult-preview-at-point-mode))
#+end_src
** Tree-sitter
#+begin_src emacs-lisp
  ;; ============================================================
  ;; Tree-sitter — distro grammars only
  ;; ============================================================
  ;; Emacs 31 is linked to libtree-sitter 0.26 (language ABI 15).
  ;; CachyOS/Arch `tree-sitter-*` packages ship ABI-15 .so files and
  ;; load from /usr/lib. Do not call `treesit-install-language-grammar`
  ;; or treesit-auto: those compile GitHub HEAD, which is what last
  ;; produced ABI mismatches. Languages without a distro grammar stay
  ;; on the classic built-in mode where one exists.
  (use-package treesit
    :ensure nil
    :config
    (setq treesit-font-lock-level 3)
    (dolist (pair '((c-mode          . c-ts-mode)
                    (c++-mode        . c++-ts-mode)
                    (c-or-c++-mode   . c-or-c++-ts-mode)
                    (python-mode     . python-ts-mode)
                    (js-mode         . js-ts-mode)
                    (javascript-mode . js-ts-mode)
                    (sh-mode         . bash-ts-mode)
                    (bash-mode       . bash-ts-mode)
                    (lua-mode        . lua-ts-mode)
                    (js-json-mode    . json-ts-mode)
                    (json-mode       . json-ts-mode)
                    (conf-toml-mode  . toml-ts-mode)
                    (toml-mode       . toml-ts-mode)
                    (elixir-mode     . elixir-ts-mode)
                    (rust-mode       . rust-ts-mode)))
      (let ((lang (pcase (cdr pair)
                    ('c-ts-mode      'c)
                    ('c++-ts-mode    'cpp)
                    ('c-or-c++-ts-mode 'cpp)
                    ('python-ts-mode 'python)
                    ('js-ts-mode     'javascript)
                    ('bash-ts-mode   'bash)
                    ('lua-ts-mode    'lua)
                    ('json-ts-mode   'json)
                    ('toml-ts-mode   'toml)
                    ('elixir-ts-mode 'elixir)
                    ('rust-ts-mode   'rust))))
        (when (and lang (treesit-language-available-p lang))
          (add-to-list 'major-mode-remap-alist pair))))
    (add-to-list 'treesit-language-source-alist
  	       '(odin "https://github.com/tree-sitter-grammars/tree-sitter-odin"))
    (add-to-list 'treesit-language-source-alist
  		 '(glsl "https://github.com/tree-sitter-grammars/tree-sitter-glsl"))
    (add-to-list 'treesit-language-source-alist
  		 '(qmljs "https://github.com/yuja/tree-sitter-qmljs")))
#+end_src

** Languages
#+begin_src emacs-lisp
  ;; =============================================================
  ;; Languages
  ;; =============================================================

  ;; C / C++
  (use-package c-ts-mode
    :ensure nil
    :mode (("\\.c\\'"   . c-ts-mode)
           ("\\.h\\'"   . c-or-c++-ts-mode)
           ("\\.cc\\'"  . c++-ts-mode)
           ("\\.cpp\\'" . c++-ts-mode)
           ("\\.cxx\\'" . c++-ts-mode)
           ("\\.c++\\'" . c++-ts-mode)
           ("\\.hh\\'"  . c++-ts-mode)
           ("\\.hpp\\'" . c++-ts-mode)
           ("\\.hxx\\'" . c++-ts-mode)
           ("\\.h++\\'" . c++-ts-mode)))

  ;; Elixir (built-in elixir-ts-mode when the grammar is present)
  (use-package elixir-ts-mode
    :ensure nil
    :if (treesit-language-available-p 'elixir)
    :mode (("\\.ex\\'"  . elixir-ts-mode)
           ("\\.exs\\'" . elixir-ts-mode)))

  ;; GLSL
  (use-package glsl-mode)

  ;; Odin
  (use-package odin-ts-mode
    :ensure (:host github :repo "Sampie159/odin-ts-mode")
    :mode "\\.odin\\'")

  ;; Python
  (use-package python
    :ensure nil
    :custom
    (python-indent-guess-indent-offset-verbose nil)
    (python-indent-offset 4))

  (use-package reformatter
    :init
    (reformatter-define python-black
      :program "black"
      :args '("-"))
    (add-hook 'python-base-mode-hook #'python-black-on-save-mode))

  ;; Rust
  (use-package rust-ts-mode
    :ensure nil
    :if (treesit-language-available-p 'rust)
    :mode "\\.rs\\'"
    :hook (rust-ts-mode . (lambda ()
                            (add-hook 'before-save-hook
                                      (lambda ()
                                        (when (eglot-managed-p)
                                          (eglot-format-buffer)))
                                      nil t))))

  ;; Zig
  (use-package zig-ts-mode)
#+end_src

** Aesthetics
#+begin_src emacs-lisp
  ;; ============================================================
  ;; Aesthetics
  ;; ============================================================
  (use-package nerd-icons :defer t)

  (use-package moody
    :init
    (setq x-underline-at-descent-line t)
    (moody-replace-mode-line-front-space)
    (moody-replace-mode-line-buffer-identification)
    (moody-replace-vc-mode))

  (use-package minions
    :init
    (minions-mode))

  (use-package gruvbox-theme
    :ensure (:wait t)
    :config
    (load-theme 'gruvbox-dark-hard t)
    ;; `load-theme' recalculates `default' from the theme spec (bg/fg only)
    ;; and drops a family set earlier in init. Apply the font after the theme.
    (set-face-attribute 'default nil
                        :family "Pragmasevka Nerd Font"
                        :height 130
                        :weight 'regular)
    (setq-default line-spacing 0.15))
#+end_src
